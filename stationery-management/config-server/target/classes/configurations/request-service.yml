server:
  port: 8083

spring:
  application:
    name: request-service
  datasource:
    url: jdbc:mysql://localhost:3306/request_db
    username: root
    password: ashuvishu
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
