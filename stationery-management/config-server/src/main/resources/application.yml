server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native #use native profile that is from local system
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations

management:
  endpoints:
    web:
      exposure:
        include: health, info
